var altura = 5;
var comprimento = 9;
area = comprimento * altura;
const forma = "Retângulo";
if (forma === "Retângulo") {
    area = altura * comprimento;
}
else{
    area = (altura * comprimento)/2;
};

console.log(area);